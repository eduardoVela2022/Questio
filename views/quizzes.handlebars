<!-- Main -->
<main class="column website-main-content">
  <!-- Main Column -->
  <div class="column website-main-content-container">
    <!-- Main Title -->
    <h1 class="row main-content-title">Your Quizzes</h1>

    <div class="row create-quiz-button-row">
      <!-- Create A Quiz Button -->
      <button id="create-quiz-button" class="btn-primary">
        Create a quiz
      </button>
    </div>

    <!-- Quiz List -->
    <ul class="column quiz-list">
      {{#each quizzes as |quiz|}}
        <!-- Quiz List Item -->
        <li class="column quiz-list-item">
          <!-- Title -->
          <h2>{{quiz.name}}</h2>

          <!-- Quiz Buttons -->
          <div class="row quiz-list-item-buttons">
            <div>
              <!-- Play Quiz Button -->
              <button
                data-quiz-id="{{quiz.id}}"
                class="btn-primary play-btn"
              >Play</button>
            </div>

            <div class="row">
              <!-- Modify Quiz Button -->
              <button
                data-quiz-id="{{quiz.id}}"
                class="btn-secondary modify-btn"
              >Modify</button>

              <!-- Delete Quiz Button -->
              <button
                data-quiz-id="{{quiz.id}}"
                data-quiz-name="{{quiz.name}}"
                class="btn-secondary delete-btn"
              >Delete</button>
            </div>
          </div>
        </li>
      {{/each}}
    </ul>
  </div>
</main>

{{! Quizzes Script }}
<script src="/public/js/quizzes.js"></script>